using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DiamondSuit : CardSuit
{
    // Start is called before the first frame update
    void Start()
    {
        suit = Suit.DIAMONDS;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
