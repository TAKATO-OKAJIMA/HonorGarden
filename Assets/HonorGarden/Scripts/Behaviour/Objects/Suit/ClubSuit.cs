using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ClubSuit : CardSuit
{
    // Start is called before the first frame update
    void Start()
    {
        suit = Suit.CLUBS;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
