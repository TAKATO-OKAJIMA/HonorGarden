using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpeadSuit : CardSuit
{
    // Start is called before the first frame update
    void Start()
    {
        suit = Suit.SPEADS;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
