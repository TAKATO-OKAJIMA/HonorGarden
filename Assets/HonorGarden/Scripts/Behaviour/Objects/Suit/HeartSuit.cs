using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HeartSuit : CardSuit
{
    // Start is called before the first frame update
    void Start()
    {
        suit = Suit.HEARTS;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
